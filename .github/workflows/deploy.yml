name: Deploy

on:
  push:
    branches:
      - develop
      - staging
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Simular deploy
        run: |
          echo "Deploy realizado no ambiente ${{ github.ref_name }}"